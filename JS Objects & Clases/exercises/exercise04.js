function solve(arr) {
    finalResult = [];

    for (let i=0; i < arr.length; i++) {
        movieObject = {}
        if ('addMovie' in arr[i]) {
                let movieName = arr[1].split(' ');
                
        } else if ('directedBy' in arr[i]) {
            ...
        } else {
            ...
        }
    }
}