function wordsTracker(arrayOfWords){
    finalResult = {};
    
    for (let i=0; i < arrayOfWords.length; i++) {
        let words = arrayOfWords.split(', ')[0];
        let occurences = arrayOfWords.split(', ')[!0];
    }
}